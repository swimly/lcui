// index.test.ts

import assert from 'assert'  
import LCDate from '../src/packages/date/index'

describe('validate:', () => {  
  /**
   * myFirstFunc
   */
  describe('date', () => {
    test(' return hello date ', () => {
      
    })
  })
})